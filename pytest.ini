[pytest]
markers =
    live: hits the OpenAI API (costs money, needs a key)

# Skip the live tests unless explicitly asked for:
#   pytest              offline tests only
#   pytest -m live      the API-backed ones
#   pytest -m ""        everything
addopts = -m "not live" --tb=short -q
